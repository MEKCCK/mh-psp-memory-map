# 贡献指南（提 PR / 提供公开内存地址）

欢迎任何**真实、可验证、来源清晰**的 PSP 怪物猎人内存地址。所有贡献合并前必须
满足下面的最低要求，否则会被打回。

## 你可以用两种方式贡献

### 方式 A：直接提 PR（推荐）

1. Fork 本仓库 → 新建分支（如 `add-p3-sharpness`）。
2. 修改 `docs/memory-map.md`（按下方"条目格式"追加/更正，保持表格结构）。
3. 提交 PR，描述里写：改了哪个游戏/版本、地址怎么来的、怎么验证的。
4. 评审通过后合并。

### 方式 B：开 Issue 提供地址

在 Issue 里按"提交模板"填写，维护者核对后帮你入表（或指出问题）。

## 条目格式（memory-map.md 表格）

每个地址条目至少包含：

| 列 | 例 |
|---|---|
| 游戏 | MHP3RD |
| Disc ID | ULJM05800 |
| 版本 | 原版 / HD（两版通用则写"双版"） |
| 绝对地址（或偏移+换算） | `0x09B4C244`（= 0x0134C244 + 0x08800000） |
| 字段 | ITEM_BOX 箱子第一格，u16 读 |
| 来源 | [item]（Kurogami2134/p3rd_item_sets） / 自挖（写明方法） |
| 验证 | 模拟器实测 / 重启后稳定 / 互证来源 |

请先看 `docs/memory-map.md` 的"来源标注"表，给条目打上正确 tag
（[orig]/[load]/[modman]/[hpbar]/[dmg]/[sharp]/[item]，自挖的注明方法）。

## Issue 提交模板

```markdown
### 内存地址提供

- 游戏：MHP3RD
- Disc ID / 版本：ULJM05800（原版）
- 绝对地址：(如 0x09B4C244；若是偏移请写原始值并说明)
- 类型/字段：u16，箱子第一格物品
- 读取/写入：读/写
- 如何获得：作者仓库链接 / 挖掘过程简述（用 of digging-guide.md 的哪种方法）
- 验证情况：□ 模拟器实测 □ 重启后稳定 □ 有第二来源
- 额外备注：
```

## 强制检查清单（合并前提）

- [ ] 能确认是 PSP 虚拟地址（绝对或标注了换算）
- [ ] 游戏与版本明确
- [ ] 来源可溯源（仓库链接 / 挖掘记录），不接收"来源不明"
- [ ] 有验证记录（哪怕一行："PPSSPP 实测改 HP 生效"）
- [ ] 地址写法没混（区分绝对 / RAM 偏移 / 宿主 base）

## 审核原则

- **宁缺毋滥**：无法验证或来源不明的条目直接拒绝。
- **鼓励互证**：同一个地址有两个独立来源时优先收录（并记录交叉验证）。
- **尊重原作**：数据源自他人项目时按 LICENSE 与 memory-map.md 的来源标注署名。
- **大改先聊**：增删整节、改结构前先开 Issue 讨论，避免冲突。

## 其他想做的事

- 想要某个游戏的某类地址？开 Issue 用 `[求地址]` 前缀。
- 有新的挖掘工具/技巧？欢迎把 [docs/digging-guide.md](docs/digging-guide.md) 补全。